i18n.merge_file(
  input: 'org.gnome.Respite.appdata.xml.in',
  output: 'org.gnome.Respite.appdata.xml',
  po_dir: '..' / '..' / 'po',
  type: 'xml',
  install: true,
  install_dir: get_option('prefix') / get_option('datadir') / 'metainfo',
)
